name: Evaluate RAG System

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  evaluate:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8'

    - name: Install dependencies
      run: |
        python -m venv venv
        . venv/bin/activate
        pip install -r requirements.txt

    - name: Set up environment variables
      run: |
        echo "PINECONE_API_KEY=${{ secrets.PINECONE_API_KEY }}" >> .env
        echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" >> .env

    - name: Download fine-tuned model
      run: |
        mkdir -p /home/moraa/Documents/10_academy/week-11/model
        wget -O /home/moraa/Documents/10_academy/week-11/model/fine-tuned-model.zip "YOUR_MODEL_FILE_URL"
        unzip /home/moraa/Documents/10_academy/week-11/model/fine-tuned-model.zip -d /home/moraa/Documents/10_academy/week-11/model

    - name: Run evaluation script
      run: |
        . venv/bin/activate
        python evaluation/evaluate_rag_system.py

    - name: Upload evaluation results
      uses: actions/upload-artifact@v2
      with:
        name: evaluation-results
        path: evaluation/results.json
